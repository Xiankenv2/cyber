#include "sentrybox/core/schema.h"
#include <string.h>

bool sentry_schema_validate(SchemaType type, const char *json_content, char *error_out, size_t error_size) {
    // Stub implementation
    return true;
}
